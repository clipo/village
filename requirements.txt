# Bayesian Radiocarbon Contemporaneity Analysis Framework
# Requirements file for R packages

# Core Dependencies
# Note: This is an R project. Install packages using:
#   install.packages(c("rcarbon", "cmdstanr", "posterior", "bayesplot", "loo",
#                      "dplyr", "ggplot2", "tidyr", "readr", "knitr"))

# Radiocarbon calibration
rcarbon >= 1.4.0

# Bayesian inference with Stan
cmdstanr >= 0.5.0
posterior >= 1.3.0
bayesplot >= 1.10.0
loo >= 2.5.0

# Data manipulation and visualization
dplyr >= 1.1.0
ggplot2 >= 3.4.0
tidyr >= 1.3.0
readr >= 2.1.0

# Document generation
knitr >= 1.42
rmarkdown >= 2.20
quarto >= 1.3

# Additional utilities
RColorBrewer >= 1.1-3
gridExtra >= 2.3
scales >= 1.2.0

# System Requirements:
# - R >= 4.2.0
# - CmdStan >= 2.32.0 (install via cmdstanr::install_cmdstan())
# - Quarto >= 1.3 (for PDF generation)
# - LaTeX distribution (for PDF output)

# Installation Instructions:
#
# 1. Install R (>= 4.2.0) from https://cran.r-project.org/
#
# 2. Install R packages:
#    install.packages(c(
#      "rcarbon", "posterior", "bayesplot", "loo",
#      "dplyr", "ggplot2", "tidyr", "readr",
#      "knitr", "rmarkdown", "RColorBrewer", "gridExtra", "scales"
#    ))
#
# 3. Install cmdstanr (requires separate steps):
#    install.packages("cmdstanr", repos = c("https://mc-stan.org/r-packages/", getOption("repos")))
#    library(cmdstanr)
#    install_cmdstan()
#
# 4. Install Quarto from https://quarto.org/docs/get-started/
#
# 5. Install LaTeX:
#    - macOS: brew install --cask mactex
#    - Linux: sudo apt-get install texlive-full
#    - Windows: https://miktex.org/download

# Verify installation:
# Run: Rscript -e "source('R/setup.R')"
